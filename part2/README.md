Infrastructure & Configuration (`run.py`, `config.py`, `requirements.txt`).

App Initialization & API (`app/__init__.py` and basic routing in `api/v1/`).

Persistence Layer & Facade (`persistence/repository.py`, `services/facade.py`).



##   Describe the purpose of each directory and file.
##   Include instructions on how to install dependencies and run the application.
